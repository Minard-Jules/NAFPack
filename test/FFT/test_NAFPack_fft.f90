! module test_NAFPack_fft

!     use NAFPack_kinds, only: dp, sp, qp, i8, i16, isp, idp
!     use NAFPack_constant, only: TOL_TEST
!     use testdrive, only: new_unittest, unittest_type, error_type, check
!     use NAFPack_meshgrid, only: meshgrid

!     implicit none(type, external)

!     private
!     public :: collect_meshgrid
! contains

! end module test_NAFPack_fft

! program test
    
!     implicit none(type, external)
    
! end program test